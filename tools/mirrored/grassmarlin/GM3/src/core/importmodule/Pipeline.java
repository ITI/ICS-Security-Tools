package core.importmodule;

/**
 * Designates the pipeline to process a given data object. Used to key the Iterator map in ImportItems
 */
public enum Pipeline {
    LOGICAL,
    PHYSICAL,
    MESH
}
